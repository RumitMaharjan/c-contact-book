# Contact Book (C)

A dynamic command-line contact manager built in C.

## Features
- Add, list, search, delete contacts
- Dynamic array using malloc/realloc — no fixed size limit
- Full name support
- Memory safe — all allocations freed on exit

## Build
gcc contact_book.c -o contact_book

## Run
./contact_book
